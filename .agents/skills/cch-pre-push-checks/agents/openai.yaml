interface:
  display_name: "DSH Pre-Push Checks"
  short_description: "Run the relevant Coco Harness checks before push"
  default_prompt: "Use $cch-pre-push-checks before pushing this Coco Harness branch."
