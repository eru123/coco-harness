# Filesystem-scenario overlay: the sandboxed filesystem stack already lives in
# the base cordis.yml, so this overlay adds only the local tool-result spill
# storage those scenarios exercise.
- id: base
  name: '@coco-harness/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: spill-local
            name: '@coco-harness/cch-spill-local'
            config:
              root: !!js process.env.CCH_SNAPSHOT_SPILL_ROOT ?? './.spill'
          - id: spill-policy
            name: '@coco-harness/cch-spill-policy'
            config:
              maxInlineBytes: !!js process.env.CCH_SNAPSHOT && 800 || 50000
