# Explicit session-query tool opt-in for the dedicated spill scenario. The
# nested filesystem overlay supplies private spill storage and its byte limit.
- id: base
  name: '@coco-harness/cordis-plugin-include'
  config:
    path: ./fs.cordis.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@coco-harness/cch-tool-session-query'
          - id: timeout-policy
            name: '@coco-harness/cch-tool-call-timeout-policy'
