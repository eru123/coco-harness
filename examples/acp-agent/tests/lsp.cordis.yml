# Exercise the model-facing LSP tool through the shipped ACP app and Loader entry path.
# The scenario workspace supplies the deterministic stdio server used by this test composition.
- id: base
  name: '@coco-harness/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - insert:
          - id: lsp
            name: '@coco-harness/cch-lsp'
          - id: lsp-stdio
            name: '@coco-harness/cch-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@coco-harness/cch-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@coco-harness/cch-tool-lsp'
            config:
              maxLocations: 1
