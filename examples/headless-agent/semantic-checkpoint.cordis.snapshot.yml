# Keyless real-Loader composition for the semantic-checkpoint recovery snapshot.
# The headless driver resumes the seeded interrupted session and emits its next
# turn over stream-json; the replay adapter supplies the deterministic response.

- id: persistence
  name: '@coco-harness/cch-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: none

- id: checkpoint
  name: '@coco-harness/cch-session-checkpoint-policy'

- id: replay
  name: '@coco-harness/cch-llm-replay'
  config:
    file: !!js process.env.CCH_SNAPSHOT_FILE
    overrideFile: !!js process.env.CCH_SNAPSHOT_OVERRIDE

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@coco-harness/cch-subprocess-local'

- id: bash
  name: '@coco-harness/cch-bash-local'
  config:
    timeoutMs: 60000

- id: agent
  name: '@coco-harness/cch-agent-spine-demo'
  config:
    agents: []
    workspaceContext: false
    skills:
      enabled: false
    toolJobs: false
    goals: false

# Await the persisted resume before the headless driver inspects root agents.
- id: resumed-agent
  name: './tests/fixtures/semantic-checkpoint-agent.ts'
