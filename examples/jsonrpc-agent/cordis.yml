# Unattended coding-agent deployment for the bundled cch-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@coco-harness/cch-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.CCH_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.CCH_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@coco-harness/cch-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@coco-harness/cch-subprocess-local'

- id: bash
  name: '@coco-harness/cch-bash-local'
  config:
    cwd: !!js process.env.CCH_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@coco-harness/cch-agent-spine-demo'
  config:
    persona: !!js process.env.CCH_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@coco-harness/cch-session-persistence-jsonl'
  config:
    root: !!js process.env.CCH_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.CCH_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@coco-harness/cch-session-checkpoint-policy'

- id: subagent
  name: '@coco-harness/cch-subagent'

- id: subagent-spawn-in-process
  name: '@coco-harness/cch-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@coco-harness/cch-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@coco-harness/cch-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@coco-harness/cch-fs-local'
  config:
    cwd: !!js process.env.CCH_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@coco-harness/cch-fs-observation-policy'

- id: tool-fs
  name: '@coco-harness/cch-tool-fs'

- id: token-meter
  name: '@coco-harness/cch-token-meter'

- id: compaction-basic
  name: '@coco-harness/cch-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
