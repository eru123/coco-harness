/**
 * @coco-harness/cch-base — the shared cch core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `cch.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @coco-harness/cch-base
 */

export {}
