[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "coco-harness-sdk"
version = "0.0.0.dev0"
description = "Python SDK for Coco Harness"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "DeepSeek" }]
dependencies = [
  "pydantic>=2.12,<3",
  "coco-harness-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/eru123/coco-harness"
Documentation = "https://github.com/eru123/coco-harness/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/eru123/coco-harness/issues"
Source = "https://github.com/eru123/coco-harness"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/coco_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
coco-harness-runtime-bin = { path = "../sdk-runtime", editable = true }
